export type CardProps = {
    src: string,
    missionLink: string,
    missionName: string,
    flightNumber: number,
    launchYear: number,
    launchSuccess: boolean,
    launchLanding: boolean,
    missionIds: string[],
}